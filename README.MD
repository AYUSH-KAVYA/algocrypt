# Crypto Algorithm Identifier

An AI/ML-based tool to identify probable cryptographic algorithms from given datasets based on implementation artifacts and metadata.

## Setup
1. Clone the repository
2. Run `pip install -r requirements.txt`